...
+ roadmap widget
  + populate namespace
  + feature and issue tables
  + item amend / comment
+ add project details box (command and double click)
  - open project path
+ global shortcut (ctrl+pgup/pgdown) -> switch between tabs
+ adjust all focuses (e.g. new project button on startup, etc.)
+ test
+ add copy notice and cleanup
+ package (deb)
+ deploy to UCS (4.99?)
...
